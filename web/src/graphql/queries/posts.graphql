query Posts($limit: Int!, $cursor: String){
  posts(cursor: $cursor, limit: $limit){
    hasMore
    posts{
      id
    	createdAt
    	updateAt
    	title
      points
      textSnippet
      creator{
        id
        username
      }
    }

  }
}